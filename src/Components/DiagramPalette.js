import React from 'react';
import './DiagramPalette.css';

const DiagramPalette = ({ onInit, onUpdateColor, onAddNode }) => {
    return (
        <div id="myPaletteDiv" className="divPallete">
        </div>
    );
};

export default DiagramPalette;
